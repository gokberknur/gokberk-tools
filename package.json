{
  "name": "gokberk-tools",
  "version": "0.0.1",
  "description": "gökberk tools — a precision cutting-tools storefront, built with SvelteKit, showcasing the @gokberknur/design-system component library.",
  "type": "module",
  "license": "MIT",
  "author": "Gökberk Nur (https://gokberk.se)",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "check:watch": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json --watch",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@gokberknur/design-system": "^0.4.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.68.0",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "svelte": "^5.56.4",
    "svelte-check": "^4.7.1",
    "typescript": "^5.9.0",
    "vite": "^8.1.0"
  }
}
